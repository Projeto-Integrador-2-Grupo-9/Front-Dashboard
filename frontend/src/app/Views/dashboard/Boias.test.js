import React from "react";
import { render } from "@testing-library/react";
import { Boias } from "./Boias";

describe("<Boias>", () => {
  test.skip("renders without crashing", () => {
    render(<Boias />);
  });
});
